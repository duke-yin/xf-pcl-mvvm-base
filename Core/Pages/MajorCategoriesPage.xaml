<?xml version="1.0" encoding="UTF-8"?>

<pages:BasePage xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:pages="clr-namespace:SunstateEquip.RentalQuotes.Core.Pages;assembly=Core" 
    xmlns:customUI="clr-namespace:SunstateEquip.RentalQuotes.Core.CustomControls;assembly=Core" 
    x:Class="SunstateEquip.RentalQuotes.Core.Pages.MajorCategoriesPage" 
    Title="RENTAL EQUIPMENT">

    <ListView ItemsSource="{Binding MajorCategories}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="80" />
                        </Grid.ColumnDefinitions>

                        <Label Grid.Column="1" Text="{Binding Name}" HorizontalOptions="Start" VerticalOptions="Center" HorizontalTextAlignment="Start" VerticalTextAlignment="Center" />

                    </Grid>

                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</pages:BasePage>